# Example voice rules — replace this file with your own

This is a placeholder. It belongs to nobody and describes no real publication.
`draft-writer` never copies it into a handoff; it checks the file is here and
names the path, so whatever does the drafting reads the rules you keep in it.

- Write example sentences in the example voice.
- Keep each example rule to one line, like these.
- Where a real rule would name a tone, an audience or a house term, this one says "example".
- Replace every line above before you ship a draft.
