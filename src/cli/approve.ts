/**
 * `warpline approve` — grant a side-effect approval for this session.
 *
 * STUB. The signature below is the final one; plan 02-06 replaces the body
 * with the `mergeGrant` write path (D-23) behind an unchanged
 * `run(argv): Promise<number>`.
 */
export async function run(_argv: string[]): Promise<number> {
  process.stderr.write('warpline approve: not implemented in this build\n')
  return 1
}
